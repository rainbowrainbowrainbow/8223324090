# Maysternya Doli — controlled cutover preparation

Date: 2026-09-12. Source checkpoint: `047e77fe4e15d36e0161f458632c4368583b0178`.
Business key: `maysternya_doli`; aliases `maysternya` and `md` are existing input aliases, not separate businesses.

**Status: PREPARATION_COMPLETE / CUTOVER_HOLD.** Task 7 remains HOLD. Real owner/membership mapping is UNPOPULATED; production preflight, traffic collection, migration and live QA are NOT_RUN. This document authorizes no protected change or production operation. It contains source findings and proposed acceptance requirements, not live counts or a claim that MD is operationally isolated.

Audit scope: read-only source inspection in `codex/sys-mb-park-dar-release-20260912`, then this documentation-only artifact in `codex/sys-mb-final-cutover-20260912`, both at the checkpoint above. No credentials, production database, provider endpoint or customer record was accessed. Referenced tests were inspected, not rerun for this audit. File/line references below are repository-relative and refer to this checkpoint.

## 1. Read-only preflight and verified source evidence

| Finding | Exact source | Consequence for MD |
| --- | --- | --- |
| Membership resolver is registry-driven, not limited to Park/Dar. It joins active organization, business and memberships and separately loads the registry. | `services/businessMembership.js:49`, `:95`, `:121`; fresh resolution at `middleware/auth.js:310` | A new MD business registry row can activate membership policy immediately. This is not sufficient proof of domain or integration readiness. |
| Public lifecycle cannot claim this historical partition. Initial bootstrap fails if any organization exists and creates only Park/Dar. Normal business creation rejects built-in keys. | `routes/organizations.js:83`, `:100`; `services/organizationLifecycle.js:181`, `:188` | Do not rerun first bootstrap, create an MD alias as a custom business, or weaken built-in validation. A separately reviewed existing-partition registration/cutover workflow is needed. |
| MD timeline is private and creator-only even for reads. Named timeline actions are creator-only; sales is denied. | `services/timelineContext.js:19`, `:29`, `:88`; consumers `routes/bookings.js:2644`, `routes/lines.js:181` | Giving an owner/director MD membership does not grant working timeline access. Removing this restriction would change current permissions and needs an exact policy decision and protected authorization. |
| The MD page permission ignores an explicit page allowlist, and frontend action lists mirror creator-only restrictions. | `config/permissionRegistry.js:207`; `js/timeline-context.js:34`; `/maysternya-doli` serves the shared timeline shell at `server.js:438` | Backend, registry, frontend and existing sidebar access consumers must be tested together. Do not grant platform `creator` as a workaround; lifecycle rejects that role and extra role at `services/organizationLifecycle.js:38`, `:47`. |
| Legacy effective access is narrower than raw user arrays. Non-creator/director roles receive only Park from the legacy context policy. | `services/businessContext.js:222`, `:224`; switch roles at `:62` | Copying every user with `maysternya_doli` in an array can expand access. Derive approved memberships from effective access, account status, page/action restrictions and approved exceptions. |
| MD compatibility modules come from built-in settings; membership modules use the registry capability list. Unsupported modules cannot become available merely by copying old module names. | `services/businessContext.js:29`; `services/businessProfile.js:71`; `services/businessModuleRegistry.js:5`, `:10`, `:25`, `:53` | Core modules and limited Omni need MD acceptance. Chat/reports/content/Kleshnya and other unmigrated modules need explicit containment or separate migration. Graduation is deliberately unavailable for MD. |
| Missing MD product IDs can be external program codes, but only before membership cutover. Known foreign products are denied. | `services/leadReferenceIntegrity.js:31`; `services/maysternyaBookingWebhook.js:1055` | Name-only booking becomes program ID `MD`; code-only input can remain an opaque external code. Switching MD to membership rejects missing product references, including replay of legacy bookings. Decide an explicit external-code/catalog mapping contract; do not seed invented products or alter protected field priority to make tests pass. |
| Universal, MD booking and MD availability webhooks use a shared provider token, without active organization/business-principal admission. MD source labels select the context. | `routes/leads.js:246`, `:299`, `:1513`, `:1560`, `:1593`, `:2431`; custom-secret boundaries `config/authBoundary.js:278`, `:285` | A human membership revoke does not revoke this credential. Bind the approved integration to a stored business/principal, with deactivation/replay policy, or establish explicit quarantine before cutover. Token values and hosting configuration stay out of this work. |
| MD booking `dryRun` is not reliably read-only for an existing external ID. Existing-booking branches run lead repair and COMMIT before the dry-run branch. | `services/maysternyaBookingWebhook.js:965`, `:991`, `:1021` | Never use this endpoint as a production read-only preflight, including with an existing ID. Verify/fix the contract only in an approved local protected scope with synthetic replay fixtures. |
| Lead notification recipients come from global user roles, not lead-business membership. | `routes/leads.js:1543`; `services/leadNotifier.js:23`, `:26`, `:48` | A new MD lead can reach configured global manager/director/creator destinations. This is an enabled source path, not evidence of actual live delivery. Notification admission, recipient ownership and retries must be closed or contained. |
| MD writes a business context into the booking outbox, but generic rule routing is not thereby tenant-authorized. | `services/maysternyaBookingWebhook.js:1138`; `services/eventBus.js:91`, `:107`, `:257` | The current admission guard is specific to `finance.income`. Rule selection is global by event type; `send_telegram` may use the global configured chat. Preserve stored event identity while specifying rule/destination ownership and behavior for queued work after cutover/deactivation. |
| Some scheduler booking notifications deliberately remain Park-scoped. | `services/scheduler.js:1345`, `:1975` use `pushDefaultTimelineBusinessContext` | Do not reinterpret their lack of MD activity as successful MD support or enable them during cutover. Inventory actual configured MD jobs separately from Park-only jobs and dormant jobs. |
| Existing shared-domain containment allows only pre-cutover Park. | `services/legacyBusinessSurface.js:25`, `:55` | MD compatibility does not reopen contained catalogs/templates/recurring/chat/intake/AI namespaces. Preserve this boundary; migration of those domains depends on approved historical mapping and public/job policies. |

The two product rows declared by `db/migrations/209_products_business_context_scope.sql:28` and `:34` are source history, not proof of current product inventory or a complete mapping of provider codes. `services/timelineResources.js:316` also defines an MD resource template. Its label and metadata do not identify a legal owner, employee membership or an approved historical-data owner.

### Required collection, before any mutation

Use a dedicated read-only connection and a bounded repeatable-read snapshot. Keep record identifiers and per-user access mappings in approved restricted storage; publish aggregate counts and artifact hashes only. No full contact payloads, usernames, tokens or notification destinations in public evidence. Record source identity, timestamp, schema/ledger fingerprint, collection completeness and unsupported queries. Missing collection is NOT_COLLECTED, never zero.

`scripts/audit-multibusiness-ownership.js:4`, `:72` already provides a dedicated-connection, counts-only, READ ONLY pattern with no apply mode. Its fixed table/edge inventory is useful but is not a complete MD membership/ingress preflight. `scripts/audit-multibusiness-readiness.js:7`, `:12` covers only seven orphan tables and prints raw user identifiers/usernames through the ordinary DB pool; it is not a safe public evidence exporter or a sufficient cutover gate.

The MD collection must include:

1. **Registry and access:** organizations/businesses; actual owner attestation; active/inactive users and memberships; existing MD registry conflicts; effective roles, extra roles, page/action allow/deny and before/after capability matrix; defaults per user/organization; multi-organization context selection; MD access that exists only through a legacy role or provider credential.
2. **Operational roots and relationships:** MD bookings, lines by date, timeline resources/settings; products; customers and customer children/social identities; leads and lead-customer/booking/program relations; tasks and assignees; finance categories/accounts/transactions and their record references; warehouse locations/stock/history/movements where applicable. Count missing, NULL, unregistered and foreign contexts, not just top-level rows. Verify actual columns and constraints first; do not assume all domains have the same schema.
3. **External program identities:** stored `bookings.program_id`/`program_code`, lead references, matching real MD products, missing external codes, conflicting real products in another business, and exact replay identity. Classify separately; a missing external code is not automatically corrupt historical data.
4. **Global/shared domains:** catalogs/templates/recurring roots and children, assets/public links, staff/payroll/certificates/Art and contractor/procurement references. Apply the common ownership decisions and restricted mapping requirements; unknown or mixed owner stays quarantined. A business context on one parent is not an owner assignment for every global referenced record.
5. **Ingress/jobs/destinations:** token-to-business authority without token values; universal and MD sources; pending/retry/event outbox counts and stored context; active rule definitions, destination ownership and retry keys; actual Omni channels and machine callers if configured for MD. Classify unavailable, configured-but-unverified, supported and dormant distinctly.

No current production counts, customer owners or integration configuration are known from this audit.

## 2. Exact mapping requirements; no inferred owner

The restricted migration input must bind to the collected source fingerprint and explicitly name the approved existing organization/business and creator/owner attestation. Do not infer a common organization from the product owner's shared login, the MD label, a username, a global role, a migration author or a provider source string. Creation of an additional organization, if required, is a separate explicit lifecycle decision.

For each account, map the intended MD membership and its role/extra roles, page/action overrides, active flag and default flag. Preserve negative overrides and inactive accounts. The acceptance invariant is no newly allowed route/action unless the owner explicitly approves that exact delegation. A raw `business_contexts` array is supporting evidence, not sufficient authority. The creator-only MD timeline policy needs its own decision: preserve restricted operation, or explicitly delegate reviewed business roles without granting platform creator privileges.

Do not silently replace an existing Park/Dar default. Migration must detect the per-user/per-organization unique active-default constraint and preserve cross-organization selection requirements. Membership schema and organization-consistency trigger already exist in `db/migrations/357_organizations_business_memberships.sql:18`, `:48`, `:66`, `:93`; source migration 356 is not this schema.

For data, map each historical root and its dependent records with the approved ownership format and decisions. Existing explicit MD contexts are evidence to review, not authority to reassign NULL/global rows. External program-code policy must explain whether codes remain external identifiers or map to exact real products; protect existing booking field/source contracts, labels, prices and formulas. OWN-01–08 remain PENDING where no approved decision exists. The actual mapping is UNPOPULATED.

## 3. Staged migration, rerun, partial-state and rollback design

This is a proposed workflow, not executable migration SQL or an approval to run it.

1. **Prepare and approve:** finish Task 7 prerequisites, collect the complete restricted preflight, approve MD policy/mapping and exact auth/provider/booking scope. Build local synthetic fixtures for MD alongside Park/Dar and a second organization. Do not edit historical migrations or allocate a new migration number from this document alone; verify the current complete filename ledger first.
2. **Implement the missing controlled registration workflow:** claim `maysternya_doli` only for the approved organization, with explicit audit and collision checks. Reuse organization ownership/lifecycle locks; reject conflicting partial registry state instead of overwriting it. Preserve ordinary custom-business creation validation and the one-time Park/Dar bootstrap.
3. **Stage without activating membership:** proposed transactional staging may create the approved registry/memberships under compatibility, then verify effects before cutover. This stage is not automatically behavior-free: resolver default selection already reads membership rows. Prove that staged defaults/context selection do not alter current sessions; otherwise keep staging outside active tables until the final transaction. No implicit owner or background resource creation from GET.
4. **Verify local migration states:** empty schema, existing organization, already-present MD compatibility registry, complete rerun, exact partial membership set, conflicting organization claim, inactive user, last-owner protection, competing default, missing external code and foreign references. Identical approved input must be a no-op on rerun; changed fingerprint/mapping or conflicting state must stop. No duplicate audit/event/resource creation or orphan child records.
5. **Atomic cutover:** after all local gates and a fresh authorized preflight, write only the approved changes and switch the exact MD registry row to membership. Recheck data/configuration fingerprints and ownership locks; do not assume request-level admission serializes an in-flight provider/job operation against cutover. Define queue drain/quarantine or equivalent synchronization and prove it with concurrency tests before activation.
6. **Post-cutover:** use actual fresh-auth HTTP/DB/browser tests for MD, unchanged Park/Dar and a second organization. Compare effective access against the approved matrix. Check same-JWT role/revoke, legacy mirror non-authority, direct foreign IDs, scoped aggregate reads, denied aggregate writes, resource creation and provider retry behavior without real external sends. Produce a new exact verification manifest.

**Rollback preflight is mandatory.** Preserve the pre-cutover registry/membership/default and legacy-mirror snapshot, approval fingerprint and exact migration journal. Prove rollback against post-cutover states including new records, revoked membership, role changes and pending accepted jobs. Simply changing `access_mode` to compatibility can restore global-role/legacy-array authority; setting `status=inactive` fails closed and is not a compatibility rollback. Neither is a generic approved rollback here.

Prefer a reviewed forward containment/fix if backward access equivalence cannot be proved. A code rollback must follow the release helper's current-branch/ancestry rules with a new forward rollback commit and exact-SHA CI. Do not deploy an old SHA directly, drop memberships/tables, erase migration ledger entries, reset other streams or reverse real operational data blindly. Historical startup migrations and prices are outside this proposed data mapping.

## 4. MD domain/readiness matrix

All statuses below are source-based preparation statuses, not newly executed live PASS/FAIL. The prior D06 MD result covers one compatibility business-profile request; it does not cover an MD membership lifecycle or certify MD operational domains.

| Domain | Current known boundary | MD cutover gate |
| --- | --- | --- |
| Account / organization / business lifecycle | Generic registry resolver exists; built-in registration is intentionally unavailable in normal creation. | HOLD: exact owner, membership/default mapping and controlled existing-partition registration workflow. |
| Timeline / booking / resources | MD private creator-only policy; shared shell and MD resource defaults. | HOLD: approved role policy, full real HTTP/browser denial and same-business behavior; protected linkedTo inherited blocker remains. |
| Products / customers / leads | Context predicates and D04 relation checks exist; MD external-code exception is compatibility-only. | HOLD: external-code decision, replay/rollback and cross-product/customer/booking tests; preserve catalog prices. |
| Tasks | Generic scoped CRUD and job callers exist. | NOT_VERIFIED_FOR_MD: fresh membership assignees, same JWT revoke, recurrence/escalation worker ownership and side effects. |
| Finance / warehouse | Core modules are listed as supported by generic registry; shared references and contractor/procurement/settlement gaps remain in Task 7. | HOLD: no registry label overrides known uncontained domains. MD financial/shared records need explicit ownership decisions and protected checks. |
| Dashboard / settings | Generic membership modules and settings exist; widgets may call shared domains. | NOT_VERIFIED_FOR_MD: correct branding/start page, empty/error states, scoped data and unavailable indirect widgets. |
| Omni / universal / MD provider ingress | Limited Omni; custom-secret MD endpoints bypass human JWT membership. | HOLD: stored principal/channel/business binding, current status/revocation, exact replay/destination contract or proved unavailable admission. |
| Notifications / booking event rules / jobs | Lead recipients and generic event destinations can be global; some scheduler features are Park-only. | HOLD: explicit MD job inventory, owned destinations, pending-work policy and executable no-send denial/retry tests. |
| Catalogs / templates / recurring / public assets | Shared namespace containment remains; historical mapping not populated. | BLOCKED_DEPENDENCY: common OWN decisions and complete private/public/background ownership; do not reopen for MD. |
| Chat / personal AI / reports / content | Registry lists unmigrated modules; chat/AI have explicit shared-surface containment, other route availability requires inspection. | HOLD: distinguish disabled UI from inaccessible API/service/job. No enabled NOT_MIGRATED domain may be certified complete. |
| HR / payroll / certificates / Art / payments | Protected shared-domain/integration scope, not included in this preparation patch. | BLOCKED_PROTECTED_SCOPE: explicit ownership policy and either safe containment or separately approved implementation. |
| Graduation | Registry enables only Park/Dar; MD is `not_migrated`. | INTENTIONALLY_UNAVAILABLE: no MD graduation feature expansion in cutover. |
| Compatibility removal | Profile fields expose mode; complete production entrypoint usage collection is unavailable. | HOLD: define observation window/coverage before collection and require both MD and CRM cutovers before common legacy removal. |

Existing executable evidence candidates to extend, not newly passed here: `tests/integration/lead-related-records-postgres.test.js:359` covers MD name-only/code-only compatibility and foreign/membership denial; existing actual-app acceptance modules cover Park/Dar plus a second organization, but need MD-specific fixtures and complete domain/provider cases. Preserve all current negative expectations until a separately approved policy changes them.

## 5. Separate release envelope prerequisites

No production envelope is active for this document. MD cutover is a separate block after stabilized Park/Dar, not a continuation of an old migration/auth permission. Required inputs are:

- a green scoped candidate with resolved enabled-domain/linkedTo blockers and current full verification;
- complete restricted MD preflight and approved mapping with no inferred owner or unclassified conflicting rows;
- exact Red authorization for reviewed auth/roles, protected booking semantics, provider principal/routing and financial/HR scope where necessary; ordinary migration/deploy permission does not cover these changes;
- a bounded migration/release envelope with exact branch/service, candidate/base SHA, full migration filenames/classification, max six hours/three attempts, previous live SHA and verified forward rollback;
- exact synthetic live fixtures, counts/TTL/cleanup registry, no real contact/provider/payment actions, and a safe MD QA path that does not treat webhook `dryRun` as read-only;
- required green CI for the pushed SHA, manual repository Railway helper, exact live SHA/branch/version/label proof, owner/worker/foreign-business QA and fixture cleanup evidence;
- measured compatibility usage across actual HTTP, machine/provider, socket and worker entrypoints, with observation period and coverage thresholds agreed before measurement. Inactive telemetry or no traffic does not prove zero usage.

**Current decision: MD_CUTOVER_READY=false; GLOBAL_MODEL_COMPLETE=false.** Next authorized local work is preparation of the exact policy/mapping and protected implementation scope above. No MD activation, production migration, compatibility deletion or new provider behavior is claimed complete.
