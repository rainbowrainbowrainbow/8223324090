# SYS-MB RECOVER-02 implementation recovery report

Status: RUNNABLE_PACKAGE_READY_WITH_PRODUCTION_PREFLIGHT_HOLD
Generated: 2026-09-13

## Scope completed

RECOVER-02 continues the existing SYS-MB candidate. It does not redesign the multibusiness model and does not apply any mapping or schema to production.

Implemented local candidate changes:

- Added a real guarded reserved-business apply path for `maysternya_doli` and `crm` in `services/businessCutover.js` and `routes/organizations.js`.
- Apply now verifies the approved mapping body against `mappingSha256`, requires a concrete `approvalRef`, checks an optional current DB fingerprint, locks the organization/cutover rows, writes business registry and memberships atomically, rejects inactive/missing mapped users, excludes platform `creator` from business roles, supports exact idempotent replay, and returns a receipt.
- Added additive catalog ownership markers in migration `364_catalog_ownership_markers.sql` for catalog roots, children, public visibility and asset/job linkage metadata. No ownership data is assigned by this migration.
- Added additive cutover receipt columns in migration `365_business_cutover_journal_approval_receipts.sql`: `approval_ref`, `source_fingerprint_sha256`, `receipt_sha256`.
- Added scoped Maysternya page access for `director`, `manager`, and `admin` only when the active membership context is `maysternya_doli`. Platform creator/global roles do not grant the page through this path.
- Added Park-owned catalog module support through the server registry while retaining non-Park catalog denial.
- Added bounded compatibility telemetry for HTTP/profile, WebSocket recipient decisions and internal legacy service decisions. The collector is `scripts/sys-mb-compatibility-telemetry-report.cjs`.
- Added a narrow `sys-mb-auth-cutover` protected release workflow in `scripts/production-block-policy.js` and `scripts/production-block-controller.js`. It only authorizes exact SYS-MB protected paths and still blocks unrelated Red surfaces.

Private approved mapping was generated outside Git under the operator private directory. Git stores only hashes/status. Production apply remains blocked until the next release task refreshes live preflight/fingerprint from the current production base.

## Approved business decisions represented

- `maysternya_doli` and `crm` belong to the current Event Genix Group organization.
- Current owner remains owner; no automatic owner assignment is performed by migration.
- Two named directors receive `director` business membership in MD/CRM.
- The operational admin account receives `admin` business membership in MD/CRM, not owner and not business creator.
- Hermes Integration Bot receives no new MD/CRM rights.
- All nine historical catalog roots remain Park-owned under `event_genix`; nothing is deleted.
- Existing public tokens are preserved only for the reviewed Park catalogs and are not regenerated by this package.
- Required modules remain visible in registry/status; unfinished domains are not hidden to manufacture a green result.
- Park/Dar memberships and Park default are not changed.
- Payroll/payment/Art remain explicitly outside automatic owner/admin expansion.

## Current production source proof

Read-only `/api/version` on 2026-09-13 returned:

- URL: `https://8223324090-production.up.railway.app`
- branch: `codex/eventgenix-production`
- commit: `487e9e872cef1455627aa0d6d31a9ef2ce9d7211`
- version: `0.81.154`
- label: `Гайдбук Design Board для реклами`

The private RECOVER-01 mapping snapshot was based on source deployment `4214598e263057b1cb1524d7fb84f328031d288d`, so production release must start from the current live SHA and rerun drift/fingerprint checks before applying mapping.

## Read-only preflight results

- Private approved mapping package: `APPROVED_PACKAGE_NOT_APPLIED`.
- Private mapping summary hash source: `458fbd72aafaf3708f2e80ad85893418d9122c0a5c7487ffe3f3de6dc12a757b`.
- Current production ownership preflight: `HOLD_OWNERSHIP_DECISIONS / INCOMPLETE_VISIBILITY`.
- Current production telemetry collector: `HOLD`, safe code `SYS_MB_TELEMETRY_REPORT_FAILED_42P01`, because the telemetry table is not present on current live production before the SYS-MB release.

## Verification completed

- `node --test tests/business-cutover.test.js` — PASS 8/8.
- `node --test tests/integration/business-cutover-journal-postgres.test.js` with disposable local PostgreSQL — PASS 2/2.
- `npm run check:migrations` — PASS.
- `npm run test:unit:business-cabinets` — PASS 84/84.
- `npm run test:sys-mb` — PASS for legacy containment, business cabinets, lead integrity and D05 domain ownership packages.
- `npm run test:browser:account-access:mobile` — PASS 1/1.
- Focused production-block controller tests including SYS-MB protected workflow — PASS within the targeted Node test run.
- Legacy/WebSocket access regression with telemetry instrumentation — PASS.

## Holds before production apply

1. Prepare a clean candidate from current production SHA `487e9e872cef1455627aa0d6d31a9ef2ce9d7211` or newer live SHA at release time.
2. Re-run read-only preflight and compute a fresh DB fingerprint after the candidate migrations are deployed.
3. Use the private approved mapping payload only if the refreshed preflight proves no drift or update the private package with a new approved hash.
4. Obtain a new exact Red/SYS-MB production block before push/deploy/apply.
5. Start compatibility measurement only after telemetry migration is live; current production cannot produce zero-usage evidence for a table it does not have.
